.view-id-external_links .view-content{
  display: grid;
  padding-left: var(--padding);
  padding-right: var(--padding);
  grid-template-columns: repeat(2, 1fr);
  grid-gap: 2rem;
  grid-template-rows: repeat(2, 1fr);
}

.view-id-external_links img{
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  transition: all .3s ease-in-out;
}
.external-link--wrapper:hover img{
  transform: translateY(-50%) scale(1.02);
}
.external-link--wrapper{
  overflow: hidden;
  position: relative;
  height: 100%;
}

.external-link--wrapper a{
  text-decoration: none;
  color: white;
  text-align: left;
  font-weight: bold;
}
.external-link--title{
  font-size: 1.33rem;
  margin-bottom: .25rem;
  font-weight: bold;
  margin-top: 1.5rem
}
.external-link--body{
  font-size: .8rem;
  margin-bottom: 2.5rem;
}
.external-link--body-container{
  background: var(--dark-blue-transparent);
  position: relative;
  max-width: 50%;
  padding: .5rem;
  height: 100%;
}